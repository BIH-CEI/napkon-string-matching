# `napkon_string_matching.types.dataset_table` Module

This module defines classes to read questionnaires from different source formats. These will be used transport this information to `DatasetTable`.
